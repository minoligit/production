import { ServersList,EditServers,CreateServers,ServerIcon} from "./getServers";

export default {
    list : ServersList,
    // show : ShowServer,
    edit : EditServers,
    create : CreateServers,
    icon : ServerIcon,
}